se esta usando el programa Python en el Visual Studio Code para realizar el codigo
usamos la libreria random en el codigo
primero escogemos las palabras que queremos que sean escogidas para este juego
usamos el random.choices para que se escoja de manera aleatoria la palabra
usamos las funciones de while y for para este codigo
usamos el break para poder detener el bucle cuando este ya haya finalizado

